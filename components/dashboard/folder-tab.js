import React from "react";

const FolderTab = () => {
  return (
    <div
      className={`absolute inset-x-4 -top-2 h2 rounded-t-md transform -skew-x-6 transition-colors ${colorClass}`}
    ></div>
  );
};

export default FolderTab;
