"use client";

import Link from "next/link";
import Image from "next/image";
import React from "react";
import { navigation, teams } from "@/constants/navbar";
import Footer from "./footer";

const Sidebar = ({ user }) => {
  return (
    <div className="flex flex-col w-64 bg-white shadow-lg p-4">
      {/* Logo */}
      <div className="text-center mb-8">
        <div className="text-2xl font-bold text-blue-500">Logo</div>
      </div>

      {/* Navigation Section */}
      <div className="mb-8">
        <h2 className="text-sm font-semibold text-gray-500">Navigation</h2>
        <ul className="space-y-4 mt-4">
          {navigation.map((item, index) => (
            <li key={index} className="flex items-center space-x-3">
              <i className={`fas fa-${item.logo} text-gray-600`} />
              <Link href={item.url} className="text-gray-700">
                {item.name}
              </Link>
            </li>
          ))}
        </ul>
      </div>

      {/* Team Section */}
      <div className="mb-8">
        <h2 className="text-sm font-semibold text-gray-500">Your Teams</h2>
        <ul className="space-y-4 mt-4">
          {teams.map((team, index) => (
            <li key={index} className="flex items-center space-x-3">
              <div className="text-gray-600 font-bold">{team.logo}</div>
              <Link href={team.url} className="text-gray-700">
                {team.name}
              </Link>
            </li>
          ))}
        </ul>
      </div>

      {/* User Section */}
      {/* <div className="flex items-center space-x-3 mt-auto">
        <Image
          src={data.user.image}
          alt={data.user.name}
          width={40}
          height={40}
          className="rounded-full"
        />
        <div>
          <div className="font-semibold text-gray-700">{data.user.name}</div>
          <div className="text-sm text-gray-500">@{data.user.github}</div>
        </div>
      </div> */}
      <Footer user={user} />
    </div>
  );
};

export default Sidebar;
